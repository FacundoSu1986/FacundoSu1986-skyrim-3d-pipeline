[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "skyrim-3d-pipeline"
version = "0.1.0.dev0"
description = "Experimental framework and Agent Skill for a Skyrim 3D asset pipeline"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
